diff --git a/src/IC/Expression.H b/src/IC/Expression.H
index 69e068bad..3e1390c50 100644
--- a/src/IC/Expression.H
+++ b/src/IC/Expression.H
@@ -34,6 +34,7 @@
 
 #ifndef IC_EXPRESSION_H_
 #define IC_EXPRESSION_H_
+#include "AMReX_Loop.H"
 #include "IC/IC.H"
 #include "Util/Util.H"
 #include "IO/ParmParse.H"
@@ -44,12 +45,11 @@ namespace IC
 {
 class Expression : public IC<Set::Scalar>, public IC<Set::Vector>
 {
-private:
+public:
     enum CoordSys { Cartesian, Polar, Spherical };
     std::vector<amrex::Parser> parser;
     std::vector<amrex::ParserExecutor<4>> f;
     Expression::CoordSys coord = Expression::CoordSys::Cartesian;
-public:
     static constexpr const char* name = "expression";
     Expression(amrex::Vector<amrex::Geometry>& _geom) : 
         IC<Set::Scalar>(_geom), IC<Set::Vector>(_geom) {}
@@ -70,31 +70,43 @@ public:
         {
             amrex::Box bx;// = mfi.tilebox();
             //bx.grow(a_field[lev]->nGrow());
+
+            const auto geom = IC<Set::Scalar>::geom[lev];
+            const Set::Vector prob_lo(geom.ProbLo());
+            const Set::Vector cell_size(geom.CellSize());
+            const auto coord = this->coord;
+
             amrex::IndexType type = a_field[lev]->ixType();
             if (type == amrex::IndexType::TheCellType())      bx = mfi.growntilebox();
             else if (type == amrex::IndexType::TheNodeType()) bx = mfi.grownnodaltilebox();
             else Util::Abort(INFO, "Unkonwn index type");
 
             amrex::Array4<Set::Scalar> const& field = a_field[lev]->array(mfi);
+            auto unitfactor = this->unitfactor;
             for (unsigned int n = 0; n < f.size(); n++)
             {
-                amrex::ParallelFor(bx, [=] AMREX_GPU_DEVICE(int i, int j, int k)
+                amrex::ParserExecutor<4> f_n = f[n];
+                amrex::ParallelFor(bx, [=] AMREX_GPU_DEVICE (int i, int j, int k)
                 {
-                    Set::Vector x = Set::Position(i, j, k, IC<Set::Scalar>::geom[lev], type);
+                    Set::Vector x = Set::Position(i, j, k, prob_lo, cell_size, type);
                     if (coord == Expression::CoordSys::Cartesian)
                     {
 #if AMREX_SPACEDIM == 1
-                        field(i, j, k, n) = f[n](x(0), 0.0, 0.0, a_time) * unitfactor;
+                        field(i, j, k, n) = f_n(x(0), 0.0, 0.0, a_time) * unitfactor;
 #elif AMREX_SPACEDIM == 2
-                        field(i, j, k, n) = f[n](x(0), x(1), 0.0, a_time) * unitfactor;
+                        field(i, j, k, n) = f_n(x(0), x(1), 0.0, a_time) * unitfactor;
 #elif AMREX_SPACEDIM == 3
-                        field(i, j, k, n) = f[n](x(0), x(1), x(2), a_time) * unitfactor;
+                        field(i, j, k, n) = f_n(x(0), x(1), x(2), a_time) * unitfactor;
 #endif
                     }
 #if AMREX_SPACEDIM>1
                     else if (coord == Expression::CoordSys::Polar)
                     {
-                        field(i, j, k, n) = f[n](sqrt(x(0)* x(0) + x(1) * x(1)), std::atan2(x(1), x(0)), x(2), a_time)  * unitfactor;
+#if AMREX_SPACEDIM == 2
+                        field(i, j, k, n) = f_n(sqrt(x(0)* x(0) + x(1) * x(1)), std::atan2(x(1), x(0)), 0.0, a_time) * unitfactor;
+#elif AMREX_SPACEDIM == 3
+                        field(i, j, k, n) = f_n(sqrt(x(0)* x(0) + x(1) * x(1)), std::atan2(x(1), x(0)), x(2), a_time) * unitfactor;
+#endif
                     }
 #endif
                 });
@@ -105,6 +117,8 @@ public:
 
     virtual void Add(const int& lev, Set::Field<Set::Vector>& a_field, Set::Scalar a_time = 0.0) override
     {
+        ALAMO_GPU_UNSUPPORTED_HOST_LOOP("IC::Expression vector");
+
         Util::Assert(INFO, TEST(a_field[lev]->nComp() == 1));
         Util::Assert(INFO, TEST(f.size() >= AMREX_SPACEDIM));
         for (amrex::MFIter mfi(*a_field[lev], amrex::TilingIfNotGPU()); mfi.isValid(); ++mfi)
@@ -118,7 +132,7 @@ public:
             Set::Patch<Set::Vector> field = a_field.Patch(lev,mfi);
             for (unsigned int n = 0; n < AMREX_SPACEDIM; n++)
             {
-                amrex::ParallelFor(bx, [=] AMREX_GPU_DEVICE(int i, int j, int k)
+                amrex::LoopConcurrentOnCpu(bx, [=] (int i, int j, int k)
                 {
                     Set::Vector x = Set::Position(i, j, k, IC<Set::Vector>::geom[lev], type);
                     if (coord == Expression::CoordSys::Cartesian)
@@ -134,7 +148,11 @@ public:
 #if AMREX_SPACEDIM>1
                     else if (coord == Expression::CoordSys::Polar)
                     {
-                        field(i, j, k)(n) = f[n](sqrt(x(0)* x(0) + x(1) * x(1)), std::atan2(x(1), x(0)), x(2), a_time)  * unitfactor;
+#if AMREX_SPACEDIM == 2
+                        field(i, j, k)(n) = f[n](sqrt(x(0)* x(0) + x(1) * x(1)), std::atan2(x(1), x(0)), 0.0, a_time) * unitfactor;
+#elif AMREX_SPACEDIM == 3
+                        field(i, j, k)(n) = f[n](sqrt(x(0)* x(0) + x(1) * x(1)), std::atan2(x(1), x(0)), x(2), a_time) * unitfactor;
+#endif
                     }
 #endif
                 });
