diff --git a/src/IC/Constant.H b/src/IC/Constant.H
index d50f11b9f..37704f285 100644
--- a/src/IC/Constant.H
+++ b/src/IC/Constant.H
@@ -7,6 +7,7 @@
 #define IC_CONSTANT_H_
 
 #include "AMReX_Config.H"
+#include "AMReX_Loop.H"
 #include "IC/IC.H"
 #include "IO/ParmParse.H"
 #include "Util/Util.H"
@@ -42,9 +43,12 @@ public:
             const amrex::Box& bx = mfi.growntilebox();
             amrex::Array4<Set::Scalar> const& field = a_field[lev]->array(mfi);
             for (int m = 0; m < a_field[lev]->nComp(); m++)
-                amrex::ParallelFor (bx,[=] AMREX_GPU_DEVICE(int i, int j, int k) {
-                field(i,j,k,m) += m_value.size() == 1 ? m_value[0] : m_value[m];
-            });  
+            {
+                Set::Scalar value = m_value.size() == 1 ? m_value[0] : m_value[m];
+                amrex::ParallelFor (bx,[=] AMREX_GPU_DEVICE (int i, int j, int k) {
+                    field(i,j,k,m) += value;
+                });
+            }
         }
     }
 
@@ -56,9 +60,13 @@ public:
             const amrex::Box& bx = mfi.growntilebox();
             amrex::Array4<Set::Vector> const& field = a_field[lev]->array(mfi);
             for (int m = 0; m < a_field[lev]->nComp(); m++)
-                amrex::ParallelFor (bx,[=] AMREX_GPU_DEVICE(int i, int j, int k) {
-                    field(i,j,k)(m) += m_value.size() == 1 ? m_value[0] : m_value[m];
-            });  
+            {
+                Set::Scalar value = m_value.size() == 1 ? m_value[0] : m_value[m];
+                amrex::ParallelFor (bx,[=] AMREX_GPU_DEVICE (int i, int j, int k)
+                {
+                    field(i,j,k)(m) += value;
+                });
+            }
         }
     }
 
