diff --git a/src/Model/Propellant/Homogenize.H b/src/Model/Propellant/Homogenize.H
index ecfdc9047..3efa3044d 100644
--- a/src/Model/Propellant/Homogenize.H
+++ b/src/Model/Propellant/Homogenize.H
@@ -1,5 +1,5 @@
 //
-// 
+//
 //
 //
 //
@@ -8,7 +8,9 @@
 #ifndef MODEL_PROPELLANT_HOMOGENIZE_H
 #define MODEL_PROPELLANT_HOMOGENIZE_H
 
+#include "IO/ParmParse.H"
 #include "Propellant.H"
+#include "Util/Util.H"
 
 namespace Model
 {
@@ -20,127 +22,130 @@ public:
     static constexpr const char* name = "homogenize";
 
     Homogenize() = default;
-    Homogenize(IO::ParmParse &pp, std::string name) 
+    Homogenize(IO::ParmParse &pp, std::string name)
     {
         pp_queryclass(name,*this);
     }
 
-    Set::Scalar P = NAN;
-    void set_pressure(const Set::Scalar P) 
+    Set::Scalar Pbar = NAN;
+    AMREX_FORCE_INLINE AMREX_GPU_HOST_DEVICE
+    void set_pressure(const Set::Scalar P)
     {
-        k4 = h1 * P + h2;
+        this->Pbar = P/P_reference;
+        k4 = h1 * Pbar + h2;
     }
-    
-    virtual Set::Scalar get_qdot(const Set::Scalar mdot, const Set::Scalar phi) 
-    {
+
+    // NOTE: device-callable evaluators below must remain free of host-only
+    // helpers (Util::Message/Util::Abort, Unit::*). Callers should flag
+    // non-finite outputs and abort after the kernel on the host.
+    AMREX_FORCE_INLINE AMREX_GPU_HOST_DEVICE
+    Set::Scalar get_qdot(const Set::Scalar mdot, const Set::Scalar phi) const    {
         Set::Scalar qflux = k4 * phi;
-        Set::Scalar mlocal = (mlocal_ap) * massfraction + (mlocal_htpb) * (1.0 - massfraction);
         Set::Scalar mdota = fabs(mdot);
-        Set::Scalar mbase = tanh(4.0 * mdota / (mlocal));
-        return  mbase * qflux;
-    }
+        Set::Scalar mbase = tanh(4.0 * mdota / (mlocal + 1.e-20));
 
-    Set::Scalar get_K (const Set::Scalar phi)
-    {
-        return (k_ap * massfraction + k_htpb * (1.0 - massfraction)) * phi + dispersion1 * (1. - phi);
+        return mbase * qflux;
     }
 
-    Set::Scalar get_rho (const Set::Scalar phi)
-    {
+    AMREX_FORCE_INLINE AMREX_GPU_HOST_DEVICE
+    Set::Scalar get_K (const Set::Scalar phi) const    {
+        return k_prop * phi + dispersion1 * (1. - phi);
+    }
 
-        return (rho_ap * massfraction + rho_htpb * (1.0 - massfraction)) * phi + dispersion2 * (1. - phi); 
+    AMREX_FORCE_INLINE AMREX_GPU_HOST_DEVICE
+    Set::Scalar get_rho (const Set::Scalar phi) const    {
+        return rho_prop * phi + dispersion2 * (1. - phi);
     }
 
-    Set::Scalar get_cp (const Set::Scalar phi)
-    {
-        return (cp_ap * massfraction + cp_htpb * (1.0 - massfraction)) * phi + dispersion3 * (1. - phi);
+    AMREX_FORCE_INLINE AMREX_GPU_HOST_DEVICE
+    Set::Scalar get_cp (const Set::Scalar phi) const    {
+        return cp_prop * phi + dispersion3 * (1. - phi);
     }
 
-    Set::Scalar get_L (Set::Scalar phi, Set::Scalar T) 
-    {
+    AMREX_FORCE_INLINE AMREX_GPU_HOST_DEVICE
+    Set::Scalar get_L (Set::Scalar phi, Set::Scalar T) const    {
         Set::Scalar L = NAN;
-        if (mob_ap == 1) L = m_ap * P * exp(-E_ap / T) * phi;
-        else L = m_ap * exp(-E_ap / T) * phi;
-        L += m_htpb * exp(-E_htpb / T) * (1.0 - phi);
-        if (T <= bound) L = 0.0;
+        if (mob_prop == 1) L = m_prop * pow(Pbar, pressure_exponent) * exp(-E_prop / T) * phi;
+        else L = m_prop * exp(-E_prop / T) * phi;
+        // Smooth quench cutoff: tanh sigmoid centered on bound with width bound_width.
+        // Avoids the bistable interface ring caused by a hard step in L.
+        Set::Scalar damping = 0.5 * (1.0 + tanh((T - bound) / bound_width));
+        L *= damping;
+
         return L;
     }
 
 
     static void Parse(Homogenize& value, IO::ParmParse& pp)
     {
-        
-        // K; dispersion variables are use to create an inert region for the void grain case. 
-        // An inert region is one that dissipates energy fast enough to remove regression and 
+
+        // K; dispersion variables are use to create an inert region for the void grain case.
+        // An inert region is one that dissipates energy fast enough to remove regression and
         // thermal strain effects.
-        pp_query("dispersion1", value.dispersion1);
-        // rho; dispersion variables are use to create an inert region for the void grain case. 
-        // An inert region is one that dissipates energy fast enough to remove regression and 
+        pp.query_required("dispersion1", value.dispersion1, Unit::ThermalConductivity());
+        // rho; dispersion variables are use to create an inert region for the void grain case.
+        // An inert region is one that dissipates energy fast enough to remove regression and
         // thermal strain effects.
-        pp_query("dispersion2", value.dispersion2);
-        // cp; dispersion variables are use to create an inert region for the void grain case. 
-        // An inert region is one that dissipates energy fast enough to remove regression and 
+        pp.query_required("dispersion2", value.dispersion2, Unit::Density());
+        // cp; dispersion variables are use to create an inert region for the void grain case.
+        // An inert region is one that dissipates energy fast enough to remove regression and
         // thermal strain effects.
-        pp_query("dispersion3", value.dispersion3); 
-
-
-        // AP Density
-        pp_query_required("rho_ap", value.rho_ap);
-        // HTPB Density
-        pp_query_required("rho_htpb", value.rho_htpb);
-        // AP Thermal Conductivity
-        pp_query_required("k_ap", value.k_ap);
-        // HTPB Thermal Conductivity
-        pp_query_required("k_htpb", value.k_htpb);
-        // AP Specific Heat
-        pp_query_required("cp_ap", value.cp_ap);
-        //HTPB Specific Heat
-        pp_query_required("cp_htpb", value.cp_htpb); 
-
-
-        // Surgate heat flux model paramater - Homogenized
-        pp_query_default("h1", value.h1, 1.81); 
-        // Surgate heat flux model paramater - Homogenized
-        pp_query_default("h2", value.h2, 1.34); 
-
-        // AP/HTPB ratio for homogenized domain
-        pp_query_default("massfraction", value.massfraction, 0.8);
-
-        // AP mass flux reference value 
-        pp_query_default("mlocal_ap", value.mlocal_ap, 0.0); 
-        value.mlocal_htpb = 685000.0 - 850e3 * value.massfraction;
-        
-        // AP Pre-exponential factor for Arrhenius Law
-        pp_query_required("m_ap", value.m_ap, Unit::Velocity()); 
-        // HTPB Pre-exponential factor for Arrhenius Law
-        pp_query_required("m_htpb", value.m_htpb, Unit::Velocity()); 
-        // AP Activation Energy for Arrhenius Law
-        pp_query_required("E_ap", value.E_ap, Unit::Energy()); 
-        // HTPB Activation Energy for Arrhenius Law
-        pp_query_required("E_htpb", value.E_htpb, Unit::Energy()); 
+        pp.query_required("dispersion3", value.dispersion3, Unit::SpecificHeatCapacity());
+
+
+        // Prop Density
+        pp.query_required("rho_prop", value.rho_prop, Unit::Density());
+        // Prop Thermal Conductivity
+        pp.query_required("k_prop", value.k_prop, Unit::ThermalConductivity());
+        // Prop Specific Heat
+        pp.query_required("cp_prop", value.cp_prop,Unit::SpecificHeatCapacity());
+
+
+        // Surrogate heat flux model parameter - Homogenized
+        pp.query_default("h1", value.h1, "1.81_W/m^2",Unit::HeatFlux());
+        // Surrogate heat flux model parameter - Homogenized
+        pp.query_default("h2", value.h2, "1.34_W/m^2",Unit::HeatFlux());
+        // Reference pressure
+        pp.query_default("P_reference", value.P_reference, "1_MPa",Unit::Pressure());
+
+        // AP mass flux reference value
+        pp.query_default("mlocal_prop", value.mlocal, "5e-1_kg/m^2/s",
+                        Unit::Mass()/Unit::Area()/Unit::Time());
+
+        // Prop Pre-exponential factor for Arrhenius Law
+        pp.query_required("m_prop", value.m_prop, Unit::Velocity());
+        // Prop Activation Energy for Arrhenius Law
+        pp.query_required("E_prop", value.E_prop, Unit::Temperature());
         // Whether to include pressure to the arrhenius law [??]
-        pp_query_default("mob_ap", value.mob_ap,false); 
+        pp.query_default("mob_prop", value.mob_prop,false);
         // HTPB Activation Energy for Arrhenius Law
-        pp_query_default("bound", value.bound,0.0); 
+        pp.query_default("bound", value.bound,"0_K", Unit::Temperature());
+        // Smoothing width for the bound cutoff (transition over ~bound_width Kelvin)
+        pp.query_default("bound_width", value.bound_width,"50_K", Unit::Temperature());
+
+        pp.query_default("pressure_exponent", value.pressure_exponent,"0.5_1", Unit::Less()); // Pressure exponent for burn-rate scaling
     }
 
     // IO variables
     Set::Scalar massfraction = NAN;
     Set::Scalar dispersion1 = NAN, dispersion2 = NAN, dispersion3 = NAN;
-    Set::Scalar k_ap = NAN, k_htpb = NAN;
-    Set::Scalar rho_ap = NAN, rho_htpb = NAN;
-    Set::Scalar cp_ap = NAN, cp_htpb = NAN;
+    Set::Scalar k_prop = NAN;
+    Set::Scalar rho_prop = NAN;
+    Set::Scalar cp_prop = NAN;
     Set::Scalar h1 = NAN, h2=NAN;
-    Set::Scalar mlocal_ap = NAN;
+    Set::Scalar mlocal = NAN;
     // Regression variables
-    bool mob_ap = -1;
-    Set::Scalar m_ap = NAN, m_htpb = NAN, m_comb = NAN;
-    Set::Scalar E_ap = NAN, E_htpb = NAN;
+    bool mob_prop = -1;
+    Set::Scalar m_prop = NAN;
+    Set::Scalar E_prop = NAN;
     Set::Scalar bound = NAN;
+    Set::Scalar bound_width = NAN;
+    Set::Scalar P_reference = NAN;
+    Set::Scalar pressure_exponent = NAN;
 
     // Calculated variables
     Set::Scalar k4 = NAN;
-    Set::Scalar mlocal_htpb = NAN;
+    Set::Scalar mlocal_prop = NAN;
 };
 
 }
@@ -149,5 +154,3 @@ public:
 
 
 #endif
-
-
