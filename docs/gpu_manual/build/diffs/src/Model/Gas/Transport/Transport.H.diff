diff --git a/src/Model/Gas/Transport/Transport.H b/src/Model/Gas/Transport/Transport.H
index 4754a0c8f..e0990238d 100644
--- a/src/Model/Gas/Transport/Transport.H
+++ b/src/Model/Gas/Transport/Transport.H
@@ -5,28 +5,70 @@
 #include <memory>
 #include <cmath>
 #include "Set/Set.H"
+#include "IO/ParmParse.H"
 
 namespace Model {
 namespace Gas {
 namespace Transport {
 
+template<typename... OBJ>
 class Transport {
-protected:
-
+private:
+    friend IO::ParmParse;
+    std::tuple<OBJ...> obj;
+    static constexpr const char* names[] = { OBJ::name... };
+    int selected = -1;
 public:
-    Transport () {} ;
-    virtual ~Transport() = default;
-    virtual const char* model_name() const = 0;
-
-    // Mixture dynamic viscosity, Pa-s
-    virtual double dynamic_viscosity(double T, Set::Patch<const Set::Scalar>& X, int i, int j, int k) const = 0;
-    // Mixture thermal conductivity coefficient, W/(m-K)
-    virtual double thermal_conductivity(double T, Set::Patch<const Set::Scalar>& X, int i, int j, int k) const = 0;
-    // Mixture species diffusion coefficients, m^2/s
-    virtual void diffusion_coeffs(
-        Set::Patch<Set::Scalar>& DKM, double T, double P,
-        Set::Patch<const Set::Scalar>& X, int i, int j, int k) = 0;
 
+    Transport() {} ;
+
+    template<int I = 0>
+    AMREX_FORCE_INLINE
+    double dynamic_viscosity(double T, Set::Patch<const Set::Scalar>& X, int i, int j, int k) const
+    {
+        if constexpr (I < sizeof...(OBJ))
+        {
+            if (selected == I) return std::get<I>(obj).dynamic_viscosity(T,X,i,j,k);
+            else return dynamic_viscosity<I+1>(T,X,i,j,k);
+        }
+        else {
+            Util::Abort(INFO, selected);
+            return NAN;
+        }
+    }
+
+
+    template<int I = 0>
+    AMREX_FORCE_INLINE
+    double thermal_conductivity(double T, Set::Patch<const Set::Scalar>& X, int i, int j, int k) const
+    {
+        if constexpr (I < sizeof...(OBJ))
+        {
+            if (selected == I) return std::get<I>(obj).thermal_conductivity(T,X,i,j,k);
+            else return thermal_conductivity<I+1>(T,X,i,j,k);
+        }
+        else {
+            Util::Abort(INFO, selected);
+            return NAN;
+        }
+    }
+
+    template<int I = 0>
+    AMREX_FORCE_INLINE
+    void diffusion_coeffs(  Set::Patch<Set::Scalar>& DKM, double T, double P,
+                            Set::Patch<const Set::Scalar>& X, int i, int j, int k) const
+    {
+        if constexpr (I < sizeof...(OBJ))
+        {
+            if (selected == I) return std::get<I>(obj).diffusion_coeffs(DKM,T,P,X,i,j,k);
+            else return diffusion_coeffs<I+1>(DKM,T,P,X,i,j,k);
+        }
+        else {
+            Util::Abort(INFO, selected);
+        }
+    }
+
+    ~Transport() = default;
 }; // class Transport
 
 } // namespace Transport
