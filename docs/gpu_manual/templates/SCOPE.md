# Port scope declaration

Schema version: 1
Port ID: `<stable-id>`
Owner: `<name>`
Date: `<YYYY-MM-DD>`

## Supported execution

- Integrator entry point:
- Executable/build target:
- Backend and toolchain:
- Dimensions:
- CPU path that must remain supported:

## Physics and capability scope

| Item | State (`in-scope`, `out-of-scope`, `unsupported`) | Rationale | Evidence/issue |
|------|---------------------------------------------------|-----------|----------------|
| `<physics/capability>` | | | |

## FEATURE/[NUM] decisions

Link the filled `FEATURE_DECISIONS.csv`. No row may remain `undecided` when
conversion begins.

## Approval

- Owner decision:
- Review evidence:
- Scope changes after freeze:

